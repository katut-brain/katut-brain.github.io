seed_captures_from_vault.py
_index_graph_backup.html
_preview_feed.html
.claude/

# secrets
.env
.env.*
*.key
credentials.json
secrets.json

# ローカルの検証・デバッグ出力(公開リポには不要)
fx_out.json
raindrop_latest.json
raindrop_top2.txt

# テスト実行で必ず生成される(test_backfill_recovery.py 等)
__pycache__/

# index.html は _build_feed.py の生成物。GitHub Actions が毎回ビルドして
# Pages へ deploy するので、リポには置かない（手で運ぶと壊れる・2026-08-31）
index.html

# 夜間Routineの所要時間計測の作業ファイル（run_timing.py が作る・成果物ではない）
.run_timing.json
.run_timing.lock

# 証跡ファイルの排他ロック（backfill.py が作る・成果物ではない）。
# ⚠️ これを追跡対象に残すと、手順8(b) の `git status --porcelain -- fetch_facts/` が
# ロックファイルだけで差分ありと判定し、押すものが無い夜に push 分岐へ入る。
fetch_facts/runs/*.lock
